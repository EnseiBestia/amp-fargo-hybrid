<!--<appfuse:lookupSelect2 cssClass="form-control" libraryPath="${'$'}{libraryPath}" id="combinedConditionValue_${field.name}" name="combinedConditionValue['${field.name}']" value="${'$'}{searchValue.combinedConditionValue['${field.name}']}" 
				            		enumCode="${pojoAnno.annoFieldUtil.getAnnoInfo(field).enumCode}" multiple="true" type="${pojoAnno.annoFieldUtil.getAnnoInfo(field).type}"/>
								
				            	<appfuse:lookupSelect2 cssClass="form-control" libraryPath="${'$'}{libraryPath}" id="combinedConditionValue_${field.name}" name="combinedConditionValue['${field.name}']" value="${'$'}{searchValue.combinedConditionValue['${field.name}']}" 
				            		propertyCode="${field.name}" multiple="true" type="${pojoAnno.annoFieldUtil.getAnnoInfo(field).type}"/>-->